# HANABIN 敵対的検証・必須修正書

- 作成日: 2026年8月9日
- 対象: `IMPLEMENTATION_PLAN.md`、`GAME_CREATOR_REVIEW.md`
- 位置づけ: 失敗、悪用、誤操作、端末差、不正利用を前提にした必須補強
- 文書優先順位: 本書 > `GAME_CREATOR_REVIEW.md` > `IMPLEMENTATION_PLAN.md`
- 状態: 実装開始前の修正版

---

## 1. 検証結論

既存計画は、通常の遊び方をするプレイヤーに対しては成立している。しかし、得点を最大化するために単純な抜け道を探すプレイヤー、意図しない操作をする初見プレイヤー、処理が遅い端末、画面比率が異なる端末、改造したクライアントを前提にすると、まだ重大な穴が残っている。

現状のまま実装すると、次の失敗が起こる可能性が高い。

1. 画面全体を雑になぞるだけで同色が集まり、精密な照準が不要になる。
2. 3個を確保したまま長時間待ち、画面が密集してから離す行動が強くなる。
3. 終盤の密度上昇を待つため、前半を捨てる行動が高得点になる。
4. 色を問わない連鎖が盤面全体へ広がり、最初の色選び以外に意味がなくなる。
5. 同じ乱数初期値でも、iPhoneとiPadで画面比率、移動距離、出現位置が変わり、同じ競技条件にならない。
6. 同時に複数の爆発が当たった場合、配列順や端末の処理順で得点と連鎖段階が変わる。
7. 安全上限へ達したときに連鎖を途中で切ると、本来の高得点だけが失われる。
8. 画面を閉じる、通知を見る、複数の指を使うなどの操作で時間や選択状態を悪用できる。
9. 集計値だけをSupabaseへ送る方式では、改造クライアントが任意の得点を登録できる。
10. 花火が独立して無作為に飛ぶだけでは、考えられる配置ではなく視覚的な雑音になりやすい。

これらを解決するまで、計画書を実装開始可能な状態とは扱わない。

---

## 2. 敵対的検証で想定するプレイヤー

通常の操作だけでなく、次の行動を必ず想定する。

- 指を高速で左右へ往復し、同色を自動的に集めようとする。
- 最初の3個を選んだ後、できるだけ長く指を離さない。
- 前半は何もせず、終盤の高密度だけを狙う。
- 画面端、ブラウザの戻る操作付近、安全領域付近だけをなぞる。
- 2本以上の指を同時に置く。
- 選択中に画面回転、通知表示、アプリ切り替えを行う。
- 同じシードを何度も覚え、最適な操作順を固定する。
- 低い表示品質を意図的に選び、見やすさや入力上の利益を得ようとする。
- Local Storageや送信内容を書き換える。
- 自動操作で最短距離の花火だけを選び続ける。

「普通はそのように遊ばない」という理由で除外しない。得点競争を入れた時点で、最も単純で有利な行動は必ず試される。

---

## 3. 最重要修正1: 画面全体をなぞるだけの操作を成立させない

### 3.1 現在の問題

違う色へ触れても無視し、同色へ触れるだけで選択へ追加できる場合、プレイヤーは花火を狙わず、画面全体を塗るようになぞればよい。これは照準、位置判断、次の色予告をすべて無意味にする。

### 3.2 必須の選択制約

MVPの初期実装では、次を採用する。

- 操作に使える指は1本だけとする。2本目以降はゲーム入力として無視する。
- 照準が花火の判定範囲へ入っただけでは即時取得しない。連続する3回の固定更新、約50ミリ秒を目安に照準内へ留まったとき取得する。
- 1回の固定更新で取得できる花火は最大1個とする。
- 候補が重なった場合は、照準中心に最も近い花火を選ぶ。同距離なら手前、さらに同じなら個別番号が小さい方を選ぶ。
- 2個目以降は、直前に選んだ花火から一定距離以内にある同色だけを取得できる。初期値はゲーム領域の対角線の28%以内とする。
- 最初の花火を選んだ時点で選択制限時間を開始する。初期値は2.5秒とする。
- 制限時間終了時に3個以上なら自動起爆し、2個以下なら解除する。
- 選択済み花火が画面外へ完全に出た場合は選択から外す。
- 違う色へ触れても選択全体は失敗しないが、その花火を次の選択距離の基準にはしない。

数値は仮値だが、「全面を高速でなぞるだけでは成立しない」という条件は変更しない。

### 3.3 合格条件

- 画面を端から端まで高速で5往復する自動操作が、狙って選ぶ操作より高得点にならない。
- 取得までの待ち時間が原因で、通常の指操作が重く感じない。
- 近い花火を順番に選ぶ経路に意味が生まれる。
- 誤って違う色へ触れただけでは全選択が失われない。

---

## 4. 最重要修正2: 長時間保持と時間切れ待ちを防ぐ

### 4.1 選択の長時間保持

選択制限時間を設けない場合、3個を確保したまま有利な配置まで待つ行動が強くなる。前項の2.5秒制限を必須とし、選択時間は得点に直接加算しない。

選択制限の残りは、照準周囲の細い輪で表示する。数字を増やさず、視線を夜空から外さない。

### 4.2 残り時間0の処理

残り時間が0になった固定更新で、入力状態を確定する。

- その更新より後の入力は受け付けない。
- 3個以上を選択中なら、その時点を指を離した扱いとして起爆する。
- 2個以下なら解除する。
- 新しい花火は出さない。
- 画面内に残っている花火の移動と、すでに始まった爆発だけを継続する。
- 最終連鎖中は `FINAL CHAIN` 相当の短い表示を出し、通常の60秒が延びたように見せない。

最終連鎖は通常の設定で2.5秒以内に必ず終わるよう、爆発寿命と連鎖遅延を設計する。2.5秒を超えた場合は安全装置の発動として扱い、黙って得点だけを切らない。

---

## 5. 最重要修正3: 色を問わない連鎖の暴走を抑える

### 5.1 現在の問題

全色が同じ強さで次の爆発へ変わる場合、花火密度が上がった終盤では、どの色を3個選んでも盤面全体が連鎖する。色を選ぶ意味が最初の3個だけになり、連鎖は運に近づく。

### 5.2 初期実装の減衰ルール

色を問わず巻き込める基本方針は残すが、次の仮値で強さを減らす。

- 直接選択した花火の爆発半径を100%とする。
- 同じ色を巻き込んだ場合は、次の爆発半径を前世代の90%とする。
- 違う色を巻き込んだ場合は、前世代の78%とする。
- 世代による減衰は下限55%までとする。
- 爆発の持続時間も世代ごとに少し短くする。
- 連鎖段階の上限は得点倍率の上限であり、通常の連鎖を途中で消すためには使わない。

第2段階では、次の2案を同じシードで比較する。

- A案: 全色を同じ強さで巻き込む。
- B案: 同色は強く、違う色は弱く連鎖する。

A案で盤面全体の自動連鎖が頻発する場合はB案を採用する。初期実装はB案から始める。

### 5.3 合格条件

- 3個を選ぶだけで、終盤の盤面が毎回ほぼ全消去されない。
- 同じ色の流れを読む価値が残る。
- 違う色へ届いたときも、連鎖が完全に止まったようには感じない。
- 大連鎖の前に起爆位置または時刻の判断が存在する。

---

## 6. 最重要修正4: 独立した無作為出現をやめ、波として設計する

### 6.1 現在の問題

花火ごとに色、位置、速度を独立して決めるだけでは、「選べるが考えられない」配置が大量に生まれる。出現保証だけを後から加えると、同色3個が機械的に固まる単調な配置になりやすい。

### 6.2 波の単位

花火を1個ずつ無関係に出すのではなく、2〜4秒程度の短い波を基本単位とする。MVPでは最低4種類の波を用意する。

| 波 | 目的 | 配置の特徴 |
|---|---|---|
| 導入波 | 3個選択を理解させる | 近い同色3個と少数の別色 |
| 分岐波 | 早く使うか育てるか選ばせる | 近い3個と、遠い4〜5個を同時に出す |
| 橋渡し波 | 連鎖位置を考えさせる | 2つの集団の間に巻き込み候補を置く |
| 交差波 | 起爆時刻を考えさせる | 異なる方向から来る集団が短時間だけ近づく |
| 圧力波 | 反応と短い判断を求める | 少数だが速い花火 |
| 終幕波 | 大連鎖の機会を作る | 複数の集団と明確な橋渡し候補 |

乱数初期値は、波の順序と波内部の調整値を決めるために使う。完全な手作業固定配置にはしないが、どの波にも狙いを持たせる。

### 6.3 出現予告の修正

「次の2色」ではなく、「次の2つの主要な波情報」として扱う。

最低限、次を表示する。

- 主要色
- 出現順
- 左、中央、右程度の大まかな方向
- 到着が近づいたことを示す短い進行表示

花火1個ごとの正確な位置は教えない。

---

## 7. 最重要修正5: 前半を捨てて終盤だけ狙う行動を防ぐ

### 7.1 現在の問題

終盤ほど花火数が増えるだけの設計では、前半に操作せず、密度が上がった最後だけ連鎖を狙う行動が効率的になる可能性がある。

### 7.2 必須対策

- 各花火に画面内での寿命を持たせ、古い花火を蓄積させない。初期値は4〜7秒とする。
- 画面内の判定対象数へ上限を設ける。
- 終盤は単純な対象数ではなく、交差、移動方向、選択肢の多さで難しさを上げる。
- 何もしなければ、画面が有利な状態へ成長し続ける設計にしない。
- 大連鎖の機会は、過去の成功によって次の波が少し豊かになる案も比較する。ただし、ランキング条件を変える動的補正は通常モードだけに限定する。

### 7.3 合格条件

30秒何もせず最後だけ操作する自動戦略の平均得点が、継続して遊ぶ基準戦略の60%を超えない。

---

## 8. 爆発予測は意図的に不正確にしない

現在の「おおよその爆発範囲」という表現は、実際の判定と表示がずれる危険がある。表示が届いているのに当たらない、表示外なのに当たる状態は、運ではなく不信感を生む。

起爆前には、直接選択した花火が作る最大半径を正確に表示する。予測表示が教えるのは半径だけであり、移動中の花火が将来どこにいるかまでは教えない。

- 判定半径と表示半径は同じ値を使う。
- 線の太さを判定へ含めない。
- 連鎖後の二次爆発範囲は事前表示しない。
- 演出品質を下げても予測線は削除しない。

これにより、表示は正確だが結果を自動で教えない状態を作る。

---

## 9. 同じシードを本当に同じ条件にする

### 9.1 固定ゲーム領域

端末の画面全体をそのままゲーム座標にすると、iPhoneとiPadで距離、密度、移動時間が変わる。ランキングへ同じシードを使うなら、内部のゲーム領域を固定する。

- 内部領域は16:9とする。
- 仮の内部座標は横16000、縦9000の整数とする。
- iPadなど比率が異なる端末では中央へ配置し、余白をHUDまたは背景へ使う。
- 花火の出現、速度、当たり判定、爆発半径は内部座標だけで計算する。
- CSS上の大きさやCanvas解像度を変えてもゲーム結果を変えない。

### 9.2 入力の正規化

端末によって指移動イベントの回数が違うため、受け取ったイベントをそのまま選択判定へ使わない。

- 指の最新位置だけを保存する。
- 固定更新ごとに1回だけ入力位置を取り込む。
- 座標を内部整数座標へ変換してから判定する。
- 記録する入力は、固定更新番号、押下状態、内部座標とする。
- 時間には端末時計ではなく単調に増える時間を使う。

同じ入力記録を再生したとき、端末の表示速度に関係なく同じ結果になることを必須とする。

---

## 10. 同時判定と連鎖の帰属を明確にする

### 10.1 1回の操作を識別する

指を離すたびに `actionId` を発行し、その操作から始まった全爆発を一つの連鎖として扱う。

各爆発は次を持つ。

- `actionId`
- 発生元の花火番号
- 親となった爆発番号
- 連鎖段階
- 発火した固定更新番号

最大連鎖は、1つの `actionId` で巻き込んだ固有花火数として計算する。

### 10.2 同時衝突

1回の固定更新では、次の二段階で処理する。

1. 更新開始時の状態を基準に、すべての衝突候補を集める。
2. 発火時刻、`actionId`、発生元番号の順で並べ、各花火の最初の1件だけを確定する。

配列を先頭から変更しながら判定しない。

同じ花火が異なる連鎖段階から同時に当たった場合は、発火時刻が早いものを採用する。同時なら小さい `actionId`、さらに同じなら発生元番号で決める。

### 10.3 連鎖中の次操作

MVPの初期ルールは次のとおりとする。

- 指を離した後、150ミリ秒だけ次の選択開始を受け付けない。
- その後は前の連鎖が続いていても次を選べる。
- 選択中の花火も既存の爆発に対して無敵にはしない。
- 既存爆発へ当たった選択花火は、前の連鎖へ帰属し、現在選択から外す。
- このとき短い表示で「先に巻き込まれた」ことを伝える。

選択した花火を無敵にすると、爆発を防ぐ盾として悪用できるため採用しない。

---

## 11. 安全上限は通常ルールとして使わない

同時爆発数や解決時間の上限へ達したとき、残りを静かに捨てると、大連鎖を作ったプレイヤーだけが損をする。

- 通常の出現数と爆発寿命で、上限へ達しないよう事前に設計する。
- 上限は無限処理や実装不具合を止める最後の安全装置とする。
- 発動したプレイは `simulationFault` として記録する。
- ローカルでは参考得点を表示してよいが、最高得点更新とランキング登録は行わない。
- 結果画面に処理異常が起きたことを明示する。
- 自動試験で安全装置が1回でも発動したシードは原因を調べる。

---

## 12. 得点式を直接選択偏重から修正する

### 12.1 現在の式の問題

`250 × (選択数 - 2)²` は、選択数が増えるほど急激に得点が増える。連鎖を考えず、6個以上を集める行動が支配的になる可能性が高いため、初期式として採用しない。

### 12.2 仮の初期式

実装開始時は、次の穏やかな増加を使う。

- 直接選択した花火: 1個100点
- 選択準備ボーナス: `120 × max(0, 選択数 - 3)`、上限600点
- 連鎖で巻き込んだ花火: `150 × (1 + 0.12 × min(連鎖段階 - 1, 6))`
- 1操作で4個以上を巻き込んだ場合: 4個目以降1個につき40点、上限800点
- 得点は各イベントごとに整数へ丸める

この式も確定値ではない。重要なのは次の得点配分である。

- 高得点プレイでは、連鎖由来の得点が総得点の55〜80%を占める。
- 直接選択由来の得点だけで、上級者の目標得点へ到達できない。
- 3個即起爆だけでも遊べるが、最上位戦略にはならない。
- 6個以上を集め続けるだけでも、最上位戦略にはならない。
- 1回の偶然の大連鎖だけで、60秒全体の判断が無意味にならない。

### 12.3 得点式の固定時期

第2段階終了時に仮式を見直し、第5段階の公開候補版で固定する。固定後は `ruleVersion` を変更しない限り式を変えない。

---

## 13. 操作中断と時間悪用への対策

### 13.1 通常プレイ

画面が非表示になった、アプリが切り替わった、画面回転が起きた場合は、自動で一時停止する。復帰時に選択状態を解除し、3秒の再開表示を出す。

### 13.2 公式ランキング

公式プレイ中に次が起きた場合は、プレイ自体は続けられてもランキング対象外にする。

- 画面が非表示になった。
- 画面回転が起きた。
- 固定更新の遅れが規定値を繰り返し超えた。
- 安全装置が発動した。
- 入力記録が欠けた。

一時停止を使って配置を考える行動を競技記録へ含めない。

### 13.3 複数の指

最初に押された指だけを有効にする。有効な指が離れるか取り消されるまで、別の指へ操作権を移さない。

`pointercancel`、`visibilitychange`、`pagehide`、画面回転では、必ず選択状態とPointer Captureを解除する。

---

## 14. 照準と画面端の修正

指より上へ固定距離で照準を出すだけでは、画面上端で照準が外へ出る。

- 基本のずらし量は画面の短辺に対する割合で決める。
- 上端に近い場合は左右へずらし、下端に近い場合は上へずらす。
- 指と照準の間を細い線で結び、どの位置を操作しているか分かるようにする。
- 照準はゲーム領域内へ固定する。
- HUDの下へ花火を出さないか、HUDが入力を遮らない配置にする。
- 画面端の花火も、判定範囲の半分以上が領域内に残るよう出現を制限する。

---

## 15. 初回導線と横画面強制の離脱を検証する

横画面専用はゲーム領域を確保しやすいが、回転を求めた時点で離脱する人がいる。縦画面で即座に無反応な状態にしない。

- 縦画面では、端末を回す短い図と理由を表示する。
- 回転後は自動で開始可能状態へ戻す。
- 最初の起動では、10〜15秒程度の操作練習を用意する。
- 操作練習は得点とランキングへ含めず、いつでも省略できる。
- 練習では3個選択、指を離す、巻き込みの3点だけを体験させる。

初見外部試遊者の20%以上が、回転案内または操作説明でゲーム開始まで進めない場合は、縦画面の簡易練習または縦向き対応を再検討する。

---

## 16. 音を完全に外したまま最終評価しない

ルール試作では音を使わなくてよい。しかし、花火の成功感は音の影響が大きいため、無音のまま公開版の感情評価を確定しない。

公開候補版では、ブラウザの音生成機能または権利を確認した独自素材を使い、最低限次の3音を任意設定として用意する。

- 選択が成立した音
- 指を離した起爆音
- 連鎖段階が上がる音

初期設定は無効とし、利用者が有効にできる。音が無効でも、光と表示だけで同じ情報を理解できるようにする。

---

## 17. 演出品質を下げても削除してはいけない情報

自動品質調整で次は削除しない。

- 選択済み花火の表示
- 対象色と選択数
- 選択制限時間
- 正確な爆発半径
- 連鎖の世代差
- 残り時間
- 次の波の主要情報

削減してよいのは、装飾粒、残像、背景反射、発光の重ね描きである。

低品質設定が花火を見やすくし、高品質より競技上有利になる可能性を試す。差がある場合、公式モードでは全端末に共通する競技表示層を固定し、装飾だけを変える。

---

## 18. 無作為試験だけでなく、攻略行動を自動化して壊す

第1段階の中で、画面を描かずに60秒を進められる簡易シミュレーターを作る。別の大きな工程には分けず、第1段階の判定試作へ含める。

最低限、次の自動戦略を用意する。

| 自動戦略 | 狙う問題 |
|---|---|
| 無作為 | 最低基準と異常終了 |
| 最短3個 | 3個即起爆の支配 |
| 6個待ち | 多数選択の支配 |
| 全面往復 | 画面なぞりの悪用 |
| 前半放置 | 終盤待ちの悪用 |
| 予告利用 | 先読みが得点へ結びつくか |
| 密集起爆 | 起爆位置の価値 |

1,000以上のシードで比較し、単純な悪用戦略が予告と位置を使う戦略を上回る場合は、演出工程へ進まない。

---

## 19. 自動試験を追加する

既存試験へ次を追加する。

### 19.1 乱数と波

- 10,000シードで、選択可能な組み合わせが規定時間以上消えない状態がない。
- 同じ波だけが長時間続かない。
- 予告した色、方向、順序と実際の波が一致する。
- 同じ場所へ選択不能なほど重なる配置を生成しない。
- 何もしない場合も対象数が上限を超えない。

### 19.2 入力

- 高速往復で大量選択できない。
- 2本目の指で別の選択を始められない。
- `pointercancel` 後に選択状態が残らない。
- 画面端で照準が領域外へ出ない。
- 同じ固定更新で複数候補が重なっても1個だけ選ぶ。

### 19.3 連鎖

- 配列順を入れ替えても同じ結果になる。
- 同時衝突の帰属が規則どおりになる。
- 同じ花火が複数の `actionId` へ重複しない。
- 選択中の花火を無敵の盾として使えない。
- 通常条件で安全装置が発動しない。

### 19.4 時間

- 端末時計を変更しても残り時間へ影響しない。
- 残り0以降の入力を取得しない。
- 背景移行から復帰しても大量の固定更新を一度に処理しない。
- 最終連鎖が規定時間内に終了する。

### 19.5 再現

- 同じシードと同じ固定更新入力で、Canvasサイズ、表示品質、表示速度を変えても得点が一致する。
- ブラウザ側とランキング検証側で同じ結果を再計算できる。

---

## 20. 試遊の合格基準を数値化する

数値は最初の小規模試遊後に調整してよいが、合否を感想だけで決めない。

### 初見プレイヤー

- 最初の成功までの中央値が12秒以内。
- 30秒時点で80%以上が基本得点方法を説明できる。
- 2個以下で解除した操作が全操作の25%を超えない。
- 横画面案内からゲーム開始まで進めない人が20%未満。

### 3回目まで

- 得点または巻き込み率のどちらかが、初回から中央値15%以上改善する。
- 次の波予告を一度以上意図的に使う人が半数以上いる。
- 「3個を見つける」以外の判断を説明できる人が半数以上いる。

### 慣れたプレイヤー

- 3個即起爆だけのプレイと6個待ちだけのプレイの両方が、混合判断より平均得点で下回る。
- 高得点者ほど、巻き込み率または起爆位置の有効性が高い。
- 同じシードでも判断の違いで結果が変わる。

最低5人の初見外部試遊者を確保できない場合、初回導線の検証は「暫定」と明記し、完成済みとは扱わない。

---

## 21. ランキングは集計値を信用しない

### 21.1 現在案の問題

得点、最大連鎖、直接選択数だけを送信して矛盾を見る方式では、改造クライアントが整合する偽データを作れる。

### 21.2 公式プレイの開始

ランキング追加時は、ゲーム開始前にSupabase側から次を受け取る。

- `runId`
- 公式シード
- `ruleVersion`
- 有効期限
- 1回限りの識別値

### 21.3 送信内容

プレイ後は、集計値だけでなく次を送る。

- `runId`
- 固定更新番号ごとの圧縮した入力記録
- 中断、回転、処理異常の有無
- クライアントが計算した参考得点

### 21.4 サーバー側の確定

Supabase側の関数またはEdge Functionで同じシードと入力を再生し、サーバーが得点を再計算する。ランキングへ保存するのはサーバー計算値だけとする。

入力記録が規則上不可能、期限切れ、二重送信、ルール版不一致、再計算不一致の場合は拒否する。

この方式でも自動操作そのものは完全には防げない。防げるのは、ゲームルールを無視した任意得点の登録である。自動操作対策と不正得点対策を混同しない。

サーバー再計算を実装しない場合、ランキングを「厳密な競技ランキング」と表現しない。

---

## 22. プレイヤー名と表示の安全性

- 名前は前後の空白を除き、1〜12文字を初期上限とする。
- 制御文字と改行を拒否する。
- 画面表示にはHTMLとして挿入せず、文字列として表示する。
- サーバー側でも同じ検査を行う。
- 同じ匿名端末からの複数記録は、上位表示では最高記録だけを使う案を採用する。
- アカウントなしでは本人性を保証できないことを前提にする。

---

## 23. 固定更新が遅れた場合の処理

固定更新を無制限に追いつかせると、処理落ち後に大量計算が発生して操作不能になる。一方、更新を捨てると競技結果が変わる。

- 表示が遅れた場合は、先に描画頻度を下げ、ゲーム計算を優先する。
- 1画面更新で処理する固定更新数へ通常上限を設ける。
- 上限を超える遅れが発生したら、次の描画を省略して計算を追いつかせる。
- 規定時間以上追いつけない端末では、通常プレイを一時停止し、低品質への変更を案内する。
- 公式プレイでは、規定回数以上の大きな遅れをランキング対象外条件とする。

更新を捨てて時間だけ進める実装は行わない。

---

## 24. 本作独自の見た目を定義する

Canvas上で粒を飛ばすだけでは、花火ライブラリの見本に見える危険がある。MVP後半では、次を本作の視覚上の特徴として育てる。

- 選択した花火を結ぶ光の線が、一時的な星座のように見える。
- 指を離した瞬間、その線を起点として複数の花火が順に開く。
- 連鎖の世代が、光の輪と残像の変化で読み取れる。
- 大連鎖後に、短い時間だけ夜空へ連鎖の形が残る。

画像、名称、画面構成をFantaVisionへ寄せず、「花火をつなぎ、夜空へ連鎖の図形を描く」ことを本作の独自性とする。

---

## 25. 実装順序への修正

既存の6段階は大きく変えない。ただし、次を必須で追加する。

### 第1段階へ含める

- 画面なしでも動く簡易シミュレーター
- 自動戦略7種
- 固定16:9内部座標
- 固定更新入力の記録と再生
- 波単位の出現
- 二段階の同時衝突処理

第1段階を別々の小さなPull Requestへ過度に分けない。判定試作として一つの動作可能なまとまりにする。

### 第2段階へ含める

- 高速全面なぞり対策
- 2.5秒の選択制限
- 正確な爆発半径表示
- 全色同強度と色別減衰の比較
- 次の波予告
- 3個、6個、放置戦略の比較

### 第3段階へ含める

- 競技上必要な表示と装飾演出の分離
- 連鎖世代の識別
- 本作独自の光の線と残像
- 安全装置発動時の異常処理

### 第4段階へ含める

- 初回の短い操作練習
- 中断復帰
- 結果画面の次回ヒント
- 名前の安全な扱い
- 任意の最低限の効果音

### 第5段階へ含める

- 10,000シード試験
- 自動戦略の比較
- 初見外部試遊
- 画面比率を変えた再生一致
- 公式表示層の公平性確認

### 第6段階へ含める

- サーバー発行の公式プレイ識別値
- 入力記録の送信
- サーバー再計算
- 通常モードと公式モードの記録分離

---

## 26. 敵対的検証後のMVP完成条件

既存条件に加え、次をすべて満たす。

1. 高速な全面なぞりが有効な攻略にならない。
2. 3個を確保したまま長時間保持できない。
3. 前半放置が継続プレイより有利にならない。
4. 全色連鎖が毎回盤面全体へ暴走しない。
5. 独立乱数ではなく、意図を持つ波が最低4種類ある。
6. 爆発半径表示と実際の判定が一致する。
7. 同じシードと入力で、端末比率と表示品質を変えても得点が一致する。
8. 同時衝突の結果が配列順へ依存しない。
9. 通常プレイで安全装置が発動しない。
10. 画面中断と複数指で時間または選択を悪用できない。
11. 3個だけ、6個だけ、放置後の一撃だけの単純戦略が最上位にならない。
12. 低品質設定が競技上の明確な利益にならない。
13. 初見外部試遊の基準を満たすか、未検証であることを明記する。
14. 公開候補版では、無音だけで最終的な成功感を評価しない。
15. 実装が花火ライブラリの見本ではなく、本作固有の選択線と連鎖表現を持つ。

---

## 27. 即時停止条件

次のいずれかが確認された場合、見た目やランキングの実装へ進まず、ルールへ戻る。

- 全面なぞり自動戦略が通常戦略を上回る。
- 3個即起爆または6個待ちだけが明確な最適解になる。
- 前半放置が有利になる。
- 連鎖の半数以上が、プレイヤーの判断と無関係な盤面全体連鎖になる。
- 同じ入力記録で結果が一致しない。
- 安全装置が通常条件で発動する。
- 初見プレイヤーが30秒後も得点方法を説明できない。
- 低品質設定の方が明確に選択しやすい。

---

## 28. 最終判断

本作で最も危険なのは、処理落ちではない。**プレイヤーが考えているように見えて、実際には全面なぞり、長時間保持、終盤待ちのどれかが最適になること**である。

実装では、花火の美しさより前に次を証明する。

- 狙って選ぶ必要がある。
- 早く起爆する判断と育てる判断の両方が存在する。
- 起爆位置と時刻が得点へ影響する。
- 単純な悪用行動より、予告と配置を読む行動が強い。
- 同じ条件を異なる端末で再現できる。

この5点を満たして初めて、花火演出、結果画面、ランキングへ進む。