# HANABIN 実装実行計画書

- 作成日: 2026年8月9日
- 対象リポジトリ: `chameleonjp-lab/hanabin`
- 状態: M7マージ済み・公開確認中
- MVP実装進捗: **6/7**（公開URL専用の実時間終端検査は成功。実機3端末、初見試遊、Pages設定画面が未確認）
- 対象範囲: MVP完成と公開まで。オンラインランキングはMVP後の別工程とする

---

## 1. この文書の目的

本書は、既存の仕様とレビューを、実際にコードへ反映する順番へ変換した実装用の計画書である。

既存文書には、ゲームの基本ルール、ゲームとしての面白さ、抜け道への対策が記載されている。本書では、それらを次の形へ整理する。

- どの順番で実装するか
- 一つのPull Requestで何を完成させるか
- 主にどのファイルを追加・変更するか
- 何を自動試験するか
- どの条件を満たせば次へ進めるか
- 何が失敗したら同じ工程内で設計へ戻るか

実装を細かく分けすぎない。MVPは7つの大きな作業単位で進め、各作業単位はブラウザまたは試験結果で確認できる状態まで完成させる。

---

## 2. 参照する文書と優先順位

### 2.1 ゲームの挙動を決める文書

ゲームのルールや挙動が競合した場合は、次の順で優先する。

1. [`ADVERSARIAL_REVIEW.md`](./ADVERSARIAL_REVIEW.md)
2. [`GAME_CREATOR_REVIEW.md`](./GAME_CREATOR_REVIEW.md)
3. [`IMPLEMENTATION_PLAN.md`](./IMPLEMENTATION_PLAN.md)

### 2.2 実装順序を決める文書

実装の順番、Pull Requestの単位、試験の実行時期は本書を基準とする。

本書はゲームルールを独自に上書きしない。既存文書と本書の内容が衝突し、挙動・得点・保存形式・試験結果へ影響する場合は、推測で実装せず、同じPull Request内で文書を修正してからコードを変更する。

---

## 3. 開発運用

### 3.1 ブランチとPull Request

- 各作業は、その時点の最新`main`から開始する。
- ブランチ名は本書で定めた名前を基本とする。
- すべてDraft Pull Requestとして作成する。
- 前のPull Requestがマージされる前に、次の実装を別ブランチへ積み重ねない。
- 一つのPull Requestには、同じ目的のコード、試験、文書をまとめる。
- 自動試験の失敗は同じブランチとPull Requestで修正する。
- Ready化、マージ、自動マージは利用者の明示指示があるまで行わない。
- `main`へ実装コードを直接追加しない。

### 3.2 作業の進捗表示

MVPの進捗は、完了した大工程数を使って表示する。

```text
MVP進捗: 0/7
```

Pull Requestを作成しただけでは完了に数えない。利用者が確認し、`main`へマージされた時点で1工程完了とする。

### 3.3 各Pull Requestに必ず書く内容

- 変更した内容
- 変更した理由
- 利用者から見える変化
- 自動試験の結果
- ブラウザ確認の結果
- 実機確認が必要な項目
- 残っている危険
- 次工程へ進めるかどうか

---

## 4. MVPの範囲

### 4.1 MVPに含める

- 横画面を基本とする60秒スコアアタック
- 4色と、色に依存しない識別補助
- 1本指による照準操作
- 同色3個以上の選択
- 選択時間制限
- 指を離す、または制限時間終了による起爆
- 同色と異色で強さが変わる連鎖
- 意図を持つ波単位の出現
- 次の2波の主要情報
- 正確な直接爆発半径の表示
- 得点、残り時間、最大連鎖、結果内訳
- 初回練習、ホーム、ゲーム、結果、再挑戦
- 端末内の設定と最高得点
- 任意で有効にできる最低限の効果音
- 3段階の装飾品質
- GitHub Pages公開
- 自動攻略、自動生成、再現性、ブラウザ操作の試験

### 4.2 MVPに含めない

- Supabaseランキング
- 利用者アカウント
- 広告、課金
- 本格的な3D描画
- 特殊能力を持つ花火
- 複数ゲームモード
- 長い物語
- 外部配信サービスからの実行時JavaScript読み込み

オンラインランキングはMVPのルールと`ruleVersion`を固定した後に開始する。

---

## 5. 技術構成

### 5.1 実行時

- HTML
- CSS
- ブラウザ標準JavaScript
- JavaScriptの標準モジュール
- Canvas 2D
- Pointer Events
- Web Audio API
- Local Storage
- GitHub Pages

公開物にはビルド処理を必須にしない。ブラウザがリポジトリ内のファイルを直接読み込める構成とする。

MVPの実行時依存は0件とする。

### 5.2 開発と自動試験

利用者の端末に開発環境を要求しない。コード編集と自動試験はGitHub上またはクラウド環境で完結させる。

- ゲーム判定: Node.jsの標準テスト機能
- ブラウザ操作試験: Playwright
- 静的配信確認: 小さなNode.js製ローカルサーバー
- 継続的な自動試験: GitHub Actions

Playwrightなどは開発時だけの依存とし、公開ゲームへ含めない。

### 5.3 設計上の境界

ゲームを次の3層へ分ける。

1. **ゲーム判定層**  
   乱数、時間、出現、選択、爆発、連鎖、得点、再生を扱う。Canvas、HTML、端末サイズを参照しない。
2. **競技表示層**  
   選択対象、選択線、選択時間、爆発半径、波予告、残り時間など、判断に必要な情報を表示する。
3. **装飾表示層**  
   光の粒、残像、背景反射、追加発光を表示する。品質を下げてもゲーム結果と競技情報を変えない。

この分離を崩し、描画の粒を当たり判定へ使ってはならない。

---

## 6. 最初に固定する実装契約

次は実装開始時の初期値である。第4工程のゲーム設計審査で比較・調整してよいが、コード内へ数値を散在させず、設定ファイル1か所で管理する。

| 項目 | 初期値 |
|---|---:|
| 内部ゲーム領域 | 16000 × 9000 |
| 内部領域の比率 | 16:9 |
| ゲーム更新 | 1秒60回 |
| 通常プレイ時間 | 3600更新、60秒 |
| 最終連鎖の通常上限 | 150更新、2.5秒 |
| 有効な指 | 最初の1本のみ |
| 花火取得までの照準保持 | 3更新、約50ミリ秒 |
| 1更新で取得できる花火 | 最大1個 |
| 次の花火を選べる距離 | 内部対角線の28%、初期値5140 |
| 選択制限時間 | 150更新、2.5秒 |
| 起爆後の次操作待ち | 9更新、150ミリ秒 |
| 花火の画面内寿命 | 240〜420更新、4〜7秒 |
| 同色を巻き込んだ次爆発 | 前世代半径の90% |
| 異色を巻き込んだ次爆発 | 前世代半径の78% |
| 世代減衰の下限 | 直接爆発半径の55% |
| 波の長さ | 120〜240更新、2〜4秒 |

### 6.1 選択数による直接爆発の初期差

| 選択数 | 半径 | 持続時間 |
|---:|---:|---:|
| 3 | 100% | 100% |
| 4 | 115% | 100% |
| 5 | 130% | 110% |
| 6以上 | 140%で上限 | 115%で上限 |

これらは比較開始用の値であり、最終値ではない。3個だけ、6個待ちだけのどちらかが最適になった場合は第4工程で変更する。

### 6.2 仮得点式

- 直接選択: 1個100点
- 選択準備ボーナス: `120 × max(0, 選択数 - 3)`、上限600点
- 連鎖捕捉: `150 × (1 + 0.12 × min(連鎖段階 - 1, 6))`
- 1操作で4個以上巻き込んだ場合: 4個目以降1個40点、上限800点
- 各得点イベントを整数へ丸めてから合計する

高得点プレイでは、連鎖由来の得点が総得点の55〜80%になることを目標とする。

### 6.3 初期値だけでは決めない項目

次は実測してから固定する。

- 基本爆発半径
- 花火の選択判定半径
- 画面内の最大花火数
- 各波の出現数と速度
- 6種類の波を混ぜる比率
- 高・中・低品質の装飾粒数

これらの決定時期と判断材料は各工程で指定する。

---

## 7. 決定的なゲーム計算

同じ乱数初期値と同じ入力記録から、端末に関係なく同じ結果を出す。

### 7.1 乱数

同じ数値から同じ並びを作る計算として、32ビット整数だけで動く`xorshift32`を初期採用する。

- ゲーム判定で`Math.random()`を使わない。
- 0を初期値にしない。0が渡された場合は固定した非0値へ置き換える。
- 色、波、位置、速度、奥行きは同じ乱数状態から生成する。
- 装飾用の粒はゲーム判定用乱数と別の乱数状態を使う。

装飾品質を変更しても、判定用乱数の消費回数を変えてはならない。

### 7.2 座標と時間

- ゲーム座標、速度、半径、時間は整数で扱う。
- 時間は固定更新番号で扱う。
- 当たり判定は中心間距離の二乗を使い、平方根を計算しない。
- 端末時計、`Date.now()`、描画フレーム数をゲーム結果へ使わない。

### 7.3 入力記録

固定更新ごとに、次だけを記録する。

- 更新番号
- 押されているか
- 有効な指の内部X座標
- 有効な指の内部Y座標
- 中断、回転、取り消しの印

ブラウザが受け取った移動イベント数を直接ゲーム判定へ使わず、最新位置を1更新に1回だけ取り込む。

### 7.4 同時衝突

1更新内では、状態を変更しながら衝突判定しない。

1. 更新開始時の状態からすべての衝突候補を集める。
2. 発火更新番号、`actionId`、発生元番号の順で候補を並べる。
3. 各花火について最初の1件だけを確定する。
4. 確定結果をまとめて状態へ反映する。

配列順を入れ替えても結果が変わらないことを自動試験する。

### 7.5 異常状態

次が起きた場合は`simulationFault`を立てる。

- 最終連鎖が安全上限を超えた
- 同時爆発数の最後の安全上限へ達した
- 不正な状態遷移を検出した
- 入力記録が欠けた
- 再生結果が一致しない

参考得点は表示してよいが、最高得点更新と将来のランキング登録は行わない。

---

## 8. 推奨ファイル構成

```text
index.html
package.json
package-lock.json
README.md
IMPLEMENTATION_EXECUTION_PLAN.md
IMPLEMENTATION_STATUS.md
styles/
  base.css
  game.css
src/
  app.js
  config/
    rules.js
    release.js
    waves.js
  core/
    constants.js
    rng.js
    coordinates.js
    input-frame.js
    state.js
    wave-generator.js
    selection.js
    chain.js
    scoring.js
    simulation.js
    replay.js
    faults.js
  game/
    controller.js
    session.js
  input/
    pointer-controller.js
  render/
    canvas-renderer.js
    competitive-layer.js
    decorative-layer.js
    firework-effects.js
    particle-pool.js
    quality-controller.js
  ui/
    screens.js
    hud.js
    orientation-guide.js
    tutorial.js
    result.js
  audio/
    sound.js
  storage/
    local-storage.js
scripts/
  serve.mjs
  check-syntax.mjs
  check-release.mjs
  run-simulations.mjs
  generate-replay-fixtures.mjs
tests/
  unit/
  simulation/
  e2e/
docs/
  GAMEPLAY_GATE_1.md
  PERFORMANCE_REPORT.md
  M6_PRODUCT_SHELL.md
  MVP_RELEASE_REPORT.md
.github/
  workflows/
    ci-core.yml
    ci-browser.yml
    pages.yml
```

ファイル名は必要に応じて変えてよい。ただし、ゲーム判定、競技表示、装飾表示の境界は維持する。

---

## 9. MVP実装の全体順序

| 工程 | ブランチ | 完成させるもの | 進行条件 |
|---|---|---|---|
| M1 | `agent/m1-foundation-ci` | 静的ページと自動試験基盤 | CIが安定して動く |
| M2 | `agent/m2-deterministic-simulator` | 画面なしで動く決定的ゲーム判定 | 再生一致と生成試験が通る |
| M3 | `agent/m3-playable-canvas` | 円表示で60秒遊べるブラウザ版 | 実入力と再生が一致する |
| M4 | `agent/m4-gameplay-hardening` | 抜け道対策とゲーム設計審査 | 単純戦略が最適でない |
| M5 | `agent/m5-visual-performance` | 独自の花火表現と性能調整 | 品質差で結果が変わらない |
| M6 | `agent/m6-product-shell` | 練習、画面、保存、結果、音 | 初回から再挑戦まで完結する |
| M7 | `agent/m7-mvp-release` | 全検証、GitHub Pages、MVP候補 | 自動検証・Pages準備は`main`へ反映済み。公開URLの本編画面まで確認済み。結果画面・実機・初見試遊待ち |

---

## 10. M1: 静的基盤と自動試験

### 10.1 目的

ゲームコードを追加する前に、ブラウザが直接読み込める構成と、自動試験を毎回実行できる状態を作る。

### 10.2 主な追加・変更

- `index.html`
- `styles/base.css`
- `src/app.js`
- `package.json`
- `package-lock.json`
- `scripts/serve.mjs`
- `scripts/check-syntax.mjs`
- `.github/workflows/ci-core.yml`
- `.github/workflows/ci-browser.yml`
- 最小限の単体試験とブラウザ起動試験

### 10.3 実装内容

- 標準モジュールで`src/app.js`を読み込む。
- ページが読み込めない場合のエラー表示を用意する。
- ビルドなしで静的配信できるようにする。
- Node.jsの標準テストを実行できるようにする。
- Playwrightでページ起動とJavaScriptエラーを確認する。
- GitHub Actionsで単体試験とブラウザ試験を分けて実行する。
- 開発用依存と公開時の実行時依存を区別する。

### 10.4 自動試験

- HTMLが読み込める。
- `src/app.js`の読み込みに失敗しない。
- ブラウザのコンソールに未処理エラーがない。
- 667×375の横画面で横スクロールが発生しない。
- 375×667の縦画面でもページ全体が壊れない。

### 10.5 完了条件

- GitHub Actionsの必須試験が成功する。
- 公開用ファイルに外部JavaScript配信先が含まれない。
- ビルドしなくても静的サーバーで表示できる。
- 実装コードを追加するためのフォルダーと試験入口が確定する。

### 10.6 対象外

- ゲームルール
- 花火描画
- 得点
- 音
- ランキング

---

## 11. M2: 決定的ゲーム判定と簡易シミュレーター

### 11.1 目的

Canvasを使わずに60秒を実行できるゲーム判定を完成させる。端末差と描画速度をゲーム結果から切り離す。

### 11.2 主な追加・変更

- `src/config/rules.js`
- `src/config/waves.js`
- `src/core/`一式
- `scripts/run-simulations.mjs`
- `scripts/generate-replay-fixtures.mjs`
- `tests/unit/`
- `tests/simulation/`

### 11.3 実装内容

- 16000×9000の内部領域
- 1秒60回の固定更新
- `xorshift32`による乱数
- 6種類の波
- 花火の寿命と画面内対象数上限
- 1本指、照準保持、1更新1取得、選択距離、選択時間制限
- `actionId`を使う爆発と連鎖
- 同色90%、異色78%の連鎖減衰
- 二段階の同時衝突処理
- 仮得点式
- 入力記録と再生
- `simulationFault`
- 描画なしで60秒を進める簡易シミュレーター
- 7種類の自動戦略

自動戦略は、無作為、最短3個、6個待ち、全面往復、前半放置、予告利用、密集起爆を実装する。

### 11.4 自動試験

- 同じ初期値と入力で同じ最終状態と得点になる。
- 入力イベント数を変えても、固定更新入力が同じなら結果が一致する。
- 花火配列の順番を変えても結果が一致する。
- 同じ花火を二重選択、二重爆発、二重加点しない。
- 2本目の指がゲーム状態を変更しない。
- 2.5秒を超えて選択を保持できない。
- 画面全体を高速往復しても1更新1取得を超えない。
- 10,000シードで生成処理が異常終了しない。
- 何もしなくても対象数上限を超えない。
- 通常条件で`simulationFault`が発生しない。
- 最終連鎖が通常2.5秒以内に終わる。

### 11.5 出力する確認資料

`docs/GAMEPLAY_GATE_1.md`の初版として、次を記録する。

- 使った設定値
- 1,000シードでの自動戦略別平均、中央値、分布
- 安全装置の発動回数
- 波ごとの出現回数
- 前半放置戦略と継続戦略の比率
- 現時点で未解決のゲーム設計上の問題

この段階では戦略間の得点差が理想的でなくてもよい。ただし、試験で再現でき、問題を数値で説明できなければ完了にしない。

### 11.6 完了条件

- ゲーム判定がCanvasとDOMを参照しない。
- 10,000シードで技術的な異常が0件である。
- 保存した入力記録の再生得点が一致する。
- 7戦略の比較結果を自動生成できる。
- 未調整項目が設定ファイルへ集約されている。

### 11.7 対象外

- 実際の指操作画面
- 花火演出
- 初回説明
- 音
- ランキング

---

## 12. M3: 円表示で遊べるブラウザ版

### 12.1 目的

M2の判定をブラウザへ接続し、仮の円表示でも60秒遊んで結果まで進める状態を作る。

### 12.2 主な追加・変更

- `src/game/controller.js`
- `src/game/session.js`
- `src/input/pointer-controller.js`
- `src/render/canvas-renderer.js`
- `src/ui/screens.js`
- `src/ui/hud.js`
- `src/ui/orientation-guide.js`
- `styles/game.css`
- `tests/e2e/`

### 12.3 実装内容

- ホーム、開始前カウント、プレイ、最終連鎖、結果の最低限の状態
- Canvasへの内部16:9領域の配置
- 画面比率が異なる場合の余白処理
- 指位置から内部座標への変換
- 最初の指だけを有効化
- Pointer Capture
- `pointercancel`、`visibilitychange`、`pagehide`、回転時の解除
- 画面端に応じて照準のずらし方向を変える
- 指と照準を線で結ぶ
- 選択中の色、数、制限時間を仮表示
- 仮の円による花火、爆発、連鎖表示
- 入力記録の保存と、その場での再生確認

### 12.4 ブラウザ試験

- 667×375の横画面
- 844×390の横画面
- 1024×768で中央16:9領域
- 375×667の縦画面案内
- マウス操作
- 2本目のPointerを無視
- `pointercancel`後に選択状態が残らない
- 画面端で照準がゲーム領域外へ出ない
- 60秒終了後に新規入力を受け付けない
- 最終連鎖後に結果へ1回だけ移る
- 記録した入力を再生すると同じ得点になる

### 12.5 実機確認

- iPhone 17 Pro Safariで横画面操作
- iPhone 11 Pro Safariで入力遅延
- iPad Pro 2018で中央配置と指移動距離

### 12.6 完了条件

- 仮表示でも開始から結果まで1回通して遊べる。
- ブラウザ操作とM2のゲーム判定が同じ結果を出す。
- 指で画面がスクロール、拡大、長押し選択されない。
- 画面回転や中断で操作が固定されたままにならない。
- 対象3端末で致命的な入力不能がない。

### 12.7 対象外

- 豪華な花火
- 最終得点式の固定
- 本格的な結果画面
- 効果音
- ランキング

---

## 13. M4: 抜け道対策とゲーム設計審査

### 13.1 目的

ゲームが動くことではなく、狙って選び、起爆位置と時刻を考える行動が最も有利であることを確認する。

この工程を通過するまで、装飾演出を優先しない。

### 13.2 主な追加・変更

- `src/config/rules.js`
- `src/core/wave-generator.js`
- `src/core/engine.js`
- `src/core/chain.js`
- `src/core/scoring.js`
- `src/render/competitive-layer.js`
- `src/ui/hud.js`
- `tests/simulation/`
- `docs/GAMEPLAY_GATE_2.md`

### 13.3 実装内容

- 選択可能距離を分かるようにする弱い表示
- 選択制限時間の輪
- 直接爆発の正確な最大半径表示
- 次の2波について、主要色、方向、順番、到着進行を表示
- 直接爆発と連鎖世代の仮表示差
- 同色・異色減衰のA/B比較
- 3個、4個、5個、6個以上の範囲差の調整
- 仮得点式の調整
- 波の出現数、速度、最大対象数の調整
- 最終連鎖の時間と表示

M4では、次波の主要色を実際に選べる5個の予告対象として現在波へ配置する。予告対象は現在波の通常対象と同時に提示し、位置を少しずらして「今の連鎖を取るか、次の波を準備するか」を選べる状態にする。予告対象を5個選んだ起爆には、予告を使った計画として固定ボーナスを加える。予告情報は主要色、左・中央・右の位置、順番、到着までの秒数をHUDへ出し、選択リンク距離と選択数に応じた直接爆発半径はコアのルール値からCanvasへ表示する。

### 13.4 自動攻略の合格条件

1,000以上の同一シード集合を使い、次を確認する。

- 予告と密集位置を使う混合戦略が、最短3個、6個待ち、全面往復、前半放置の各戦略より平均得点と中央値の両方で上回る。
- 同点またはほぼ同じ結果で、判断の差が得点へ出ていない場合も不合格とする。
- 前半30秒を放置する戦略は、継続戦略の平均得点の60%を超えない。
- 全面往復戦略が狙って選択する戦略を上回らない。
- 盤面の大半を自動的に消す連鎖が、全連鎖の半数以上にならない。
- 高得点側では連鎖由来得点が総得点の55〜80%を目安とする。
- 通常条件で`simulationFault`が0件である。

### 13.5 人による確認

同じシードを使い、次の遊び方を比較する。

- 3個で毎回すぐ起爆
- 可能な限り6個まで待つ
- 予告と密集位置を見て3〜6個を使い分ける

混合判断が最も強く感じられない場合は、数値または波を同じPull Request内で調整する。

### 13.6 即時停止条件

次のどれかが起きた場合、M5へ進まない。

- 全面往復が有利
- 長時間保持に近い行動が有利
- 3個即起爆だけが最適
- 6個待ちだけが最適
- 前半放置が有利
- 起爆位置と時刻を考えなくても大連鎖する
- 爆発半径表示と実判定が不一致
- 同じ入力記録で結果が一致しない
- 通常条件で安全装置が発動

### 13.7 完了条件

- `docs/GAMEPLAY_GATE_2.md`へ採用値と比較結果を記録する。
- 単純戦略が最上位でない。
- 波予告を使う価値が数値と試遊の両方で確認できる。
- 早く起爆する場面と育てる場面が両方ある。
- 第5工程へ進めると明記できる。

---

## 14. M5: 本作独自の花火表現と性能対策

### 14.1 目的

ゲーム判定を変えず、成功の大きさと連鎖の世代を読み取れる花火表現を追加する。

### 14.2 主な追加・変更

- `src/render/competitive-layer.js`
- `src/render/decorative-layer.js`
- `src/render/firework-effects.js`
- `src/render/particle-pool.js`
- `src/render/quality-controller.js`
- `styles/game.css`
- `docs/PERFORMANCE_REPORT.md`
- 表示品質と性能の試験

M5では、判定層・競技表示層へ装飾用の状態を戻さない。光の粒、残像、発光、連鎖到達時の反応は、`DecorativeLayer`がCanvas描画時だけに保持する。装飾品質を変更しても、固定更新入力、得点、乱数、再生ログ、結果画面の値は変更してはならない。

装飾品質は高・中・低の3段階とし、品質ごとに次だけを変える。

- Canvasの内部解像度倍率
- 背景の星の数
- 爆発1回あたりの光粒数
- 発光の重ね描き量

競技表示層の対象、選択線、選択数、制限時間、正確な爆発半径、連鎖情報、残り時間、次波予告は品質変更で削除しない。画面更新時間が一定期間遅い場合だけ品質を一段下げ、十分に速い状態が続いた場合に一段戻す。ゲーム時計を品質制御へ渡してはならない。

### 14.3 実装内容

- 選択した花火を結ぶ、星座のような光の線
- 指を離したとき、選択線に沿って順に開く直接爆発
- 放射状と円環状の花火
- 連鎖世代で変わる輪、残像、明るさ
- 大連鎖後に短く残る連鎖形状
- 5、10、20、30連鎖の成功反応
- 光の粒の再利用
- 高・中・低の装飾品質
- 表示が遅い場合の自動品質調整
- 内部Canvas解像度倍率の上限
- 競技表示を品質変更から保護

### 14.4 削減してよいもの

- 装飾粒
- 背景反射
- 残像の長さ
- 発光の重ね描き

### 14.5 削減してはいけないもの

- 選択対象
- 選択線
- 対象色と選択数
- 選択制限時間
- 正確な爆発半径
- 連鎖世代差
- 残り時間
- 次の波情報

### 14.6 性能試験

- 高・中・低品質で同じ入力記録の得点が一致する。
- Canvas解像度を変えても得点が一致する。
- iPhone 17 Proで通常時60fps付近を目標にする。
- 大連鎖で250ミリ秒を超える停止を繰り返さない。
- iPhone 11 Proで30fps未満が長時間続かない。
- 10回連続プレイで目立った入力遅延とメモリ増加がない。
- 低品質の方が選択しやすくなっていないか確認する。

### 14.7 完了条件

- 装飾品質を変えてもゲーム結果が同じである。
- 大連鎖が小連鎖と明確に異なる成功反応を返す。
- 連鎖の世代が画面から読み取れる。
- 花火ライブラリの見本ではなく、本作独自の選択線と残像がある。
- 対象3端末でプレイを継続できる。
- `docs/PERFORMANCE_REPORT.md`へ測定結果を記録する。

---

## 15. M6: 製品画面、初回練習、保存、結果、音

### 15.1 目的

初めて開いた人が操作を理解し、60秒遊び、結果を見て次のプレイへ進める製品状態を作る。

### 15.2 主な追加・変更

- `src/ui/screens.js`
- `src/ui/orientation-guide.js`
- `src/ui/tutorial.js`
- `src/ui/result.js`
- `src/audio/sound.js`
- `src/storage/local-storage.js`
- ホーム、結果、共有、保存のE2E試験

### 15.3 実装内容

- ホーム画面
- 横向き案内
- 10〜15秒の初回練習
- 練習の省略
- 3秒の中断復帰表示
- プレイヤー名入力
- 端末内最高得点と最大連鎖
- 演出品質設定
- 音の有効・無効設定。初期値は無効
- 選択成立音、起爆音、連鎖上昇音
- 結果の内訳
- プレイ内容から一つだけ返す次回ヒント
- 再挑戦
- ホームへ戻る
- 共有文。文末に公開URLを含める

### 15.4 名前の扱い

- 前後の空白を除く。
- 1〜12文字を初期範囲とする。
- 改行と制御文字を拒否する。
- HTMLとして挿入せず、文字列として表示する。
- 壊れた保存データは安全な初期値へ戻す。

### 15.5 中断処理

通常プレイでは、非表示、アプリ切り替え、画面回転で自動停止する。復帰時は現在選択を解除し、3秒の再開表示を行う。

`pointercancel`、`visibilitychange`、`pagehide`、回転時にPointer Captureと選択状態を必ず解除する。

### 15.6 自動試験

- 初回練習を完了できる。
- 初回練習を省略できる。
- 縦画面から横画面へ変えると開始可能状態へ戻る。
- 中断復帰時に選択が残らない。
- 名前へHTML文字列を入れてもHTMLとして実行されない。
- 保存データ破損時に起動できる。
- 最高得点を正しく更新する。
- 結果ヒントを一つだけ表示する。
- 共有文の末尾にURLがある。
- 音が無効でも同じ情報を画面から理解できる。

### 15.7 完了条件

- 初回起動から再挑戦まで迷わず進める。
- 音の初期値が無効である。
- 画面中断を使って配置を考える利益が残らない。
- 名前と保存データを安全に扱う。
- 結果画面から次に変える行動を一つ理解できる。

---

## 16. M7: 総合検証とMVP公開

### 16.1 目的

技術的に動くだけでなく、ゲームとして成立し、対象端末で公開できることを確認する。

### 16.2 主な追加・変更

- 10,000シードの最終試験
- 1,000シード以上の自動戦略比較
- 全ブラウザE2E
- `src/config/release.js`による公開版固定情報
- `scripts/check-release.mjs`による公開前の構成検査
- 対象端末の連続プレイ
- GitHub Pagesの公開設定
- `docs/MVP_RELEASE_REPORT.md`
- `ruleVersion`と保存形式版の固定
- 必要な公開説明

### 16.3 自動試験の最終条件

- 10,000シードで生成不能と`simulationFault`が0件。
- 7種類の戦略比較でM4の合格条件を維持する。
- 同じ入力記録を、異なるCanvasサイズと品質で再生して同じ得点になる。
- 配列順を変えても同じ結果になる。
- 60秒終了、最終連鎖、結果確定が二重実行されない。
- 375×667の縦画面案内と667×375の小型横画面を確認する。
- 主要な横画面とiPad比率で横スクロールがない。
- 公開経路からすべての必要ファイルを読み込める。

### 16.4 実機試験

#### iPhone 17 Pro

- 10回連続プレイ
- 大連鎖
- 通知、アプリ切り替え、復帰
- 画面回転
- 高品質と自動品質調整
- 結果共有

#### iPhone 11 Pro

- 中・低品質
- 入力遅延
- 大連鎖の停止
- 10回連続プレイ

#### iPad Pro 2018

- 中央16:9領域
- 余白とHUD
- 指移動距離
- 花火密度と視認性

### 16.5 初見試遊

最低5人の初見外部試遊者で次を確認する。

- 最初の成功までの中央値が12秒以内。
- 30秒時点で80%以上が基本得点方法を説明できる。
- 2個以下で解除した操作が全操作の25%を超えない。
- 横画面案内から開始できない人が20%未満。
- 3回目までに、得点または巻き込み率の中央値が15%以上改善する。
- 半数以上が次の波予告を意図的に使う。

5人を確保できない場合、GitHub Pagesへ公開試験版を出すことはできるが、`MVP完成`とは記録しない。進捗は`6/7・公開試験中`とする。

### 16.6 公開条件

- 既存3文書と本書の必須条件を満たす。
- 即時停止条件が残っていない。
- ゲームルールと得点式を`ruleVersion`へ固定する。
- 試験結果と未解決事項を`docs/MVP_RELEASE_REPORT.md`へ記載する。
- GitHub Pagesでホームから結果まで動作する。
- 実験中または暫定の項目を、完成済みと誤記しない。

### 16.7 完了条件

- 自動試験、E2E、実機試験、初見試遊がすべて完了する。
- GitHub Pagesの公開URLで遊べる。
- `docs/MVP_RELEASE_REPORT.md`に根拠が残る。
- `IMPLEMENTATION_STATUS.md`を7/7へ更新する。

### 16.8 M7マージ後の状態

Pull Request [#11](https://github.com/chameleonjp-lab/hanabin/pull/11) は2026年8月11日に `main` へマージされ、マージコミットは `942a28441549305f4e1d8c57535f9c87de695db7` である。M7ではゲームルールと判定値を変更せず、公開候補の自動検証とGitHub Pages準備を確定した。

- [CI Core #29](https://github.com/chameleonjp-lab/hanabin/actions/runs/31481047136): Node 22、Node 24、全量シミュレーションが成功。
- [CI Browser #29](https://github.com/chameleonjp-lab/hanabin/actions/runs/31481047138): Chromium E2E 24/24が成功。
- [シミュレーション証跡Artifact](https://github.com/chameleonjp-lab/hanabin/actions/runs/31481047136/artifacts/9097408031): `m2-simulation-report`を保存済み。
- 2026年8月12日、公開URL [https://chameleonjp-lab.github.io/hanabin/](https://chameleonjp-lab.github.io/hanabin/)を確認した。直アクセスでホームを表示し、「ゲームを開始」→初回練習→スキップ→カウントダウン→本編Canvas/HUD（残り時間、得点、連鎖、次の2波予告）まで到達した。
- 公開URL専用Actionsで本編から結果画面まで到達した。実機3端末、初見5人、Pages設定画面の確認は未実施であり、MVP完成とは記録しない。
- R1公式プレイとR2ランキングは、MVP完成と利用者の開始指示まで保留する。

### 16.9 PR #14マージ後の確認

Pull Request [#14](https://github.com/chameleonjp-lab/hanabin/pull/14)は、マージコミット `a9441c65b6900fd2b18b7a9fc10bd7499062885c`で`main`へ反映済みである。ゲームコードとルールは変更していない。

- [CI Core #35](https://github.com/chameleonjp-lab/hanabin/actions/runs/31518762135)と[CI Browser #35](https://github.com/chameleonjp-lab/hanabin/actions/runs/31518762120)が成功した。
- 公開URLの静的エントリーはHTTP 200を返し、`HANABIN`のHTMLと公開版固定情報を読み込める。
- HTTP 200と自動E2Eの成功は、公開URL上で人がクリックして結果画面まで到達した証拠とは分けて扱う。
- 公開URLの結果画面、実機3端末、初見5人、Pages設定画面は未確認のため、MVPは6/7のままとする。


### 16.10 PR #20マージ後の記録同期

Pull Request [#20](https://github.com/chameleonjp-lab/hanabin/pull/20)は2026年8月12日に`main`へマージされ、マージコミットは`25c0fc7e35292098c2c09a0878760944591e27a4`である。PR #20は、PR #19のマージ結果とCI成功、M7手動受入未完了を、計画書・進捗・README・公開候補レポートへ同期した文書のみの変更である。

- [CI Core #47](https://github.com/chameleonjp-lab/hanabin/actions/runs/31549096595)は成功した。
- [CI Browser #47](https://github.com/chameleonjp-lab/hanabin/actions/runs/31549096537)は成功した。
- ゲームコード、ゲームルール、得点、保存形式、公開artifactは変更していない。
- 実機3端末、初見5人、Pages設定画面は未確認のため、MVP 6/7とR1/R2保留を維持する。

### 16.11 PR #21マージ後の記録同期

Pull Request [#21](https://github.com/chameleonjp-lab/hanabin/pull/21)は2026年8月12日に`main`へマージされ、マージコミットは`f52325024cd0bb2601684550b3953f676afab627`である。PR #21は、PR #20のマージ結果とCI成功、M7手動受入未完了を、計画書・進捗・README・公開候補レポートへ同期した文書のみの変更である。

- [CI Core #49](https://github.com/chameleonjp-lab/hanabin/actions/runs/31551055627)は成功した。
- [CI Browser #49](https://github.com/chameleonjp-lab/hanabin/actions/runs/31551055613)は成功した。
- ゲームコード、ゲームルール、得点、保存形式、公開artifactは変更していない。
- 実機3端末、初見5人、Pages設定画面は未確認のため、MVP 6/7とR1/R2保留を維持する。


### 16.12 PR #22マージ後の記録同期

Pull Request [#22](https://github.com/chameleonjp-lab/hanabin/pull/22)は2026年8月12日に`main`へマージされ、マージコミットは`5d18a02ca23c793aef6a7b50484bad1cc80de1ce`である。PR #22は、PR #21のマージ結果とCI成功、M7手動受入未完了を、計画書・進捗・README・公開候補レポートへ同期した文書のみの変更である。

- [CI Core #51](https://github.com/chameleonjp-lab/hanabin/actions/runs/31553411376)は成功した。
- [CI Browser #51](https://github.com/chameleonjp-lab/hanabin/actions/runs/31553411462)は成功した。
- ゲームコード、ゲームルール、得点、保存形式、公開artifactは変更していない。
- 実機3端末、初見5人、Pages設定画面は未確認のため、MVP 6/7とR1/R2保留を維持する。


### 16.13 PR #23マージ後の記録同期

Pull Request [#23](https://github.com/chameleonjp-lab/hanabin/pull/23)は2026年8月12日に`main`へマージされ、マージコミットは`aea8eeb2425a2f4bcf82e9aafd2401ff1bc55e84`である。PR #23は、PR #22のマージ結果とCI成功、M7手動受入未完了を、計画書・進捗・README・公開候補レポートへ同期した文書のみの変更である。

- [CI Core #53](https://github.com/chameleonjp-lab/hanabin/actions/runs/31554230532)は成功した。
- [CI Browser #53](https://github.com/chameleonjp-lab/hanabin/actions/runs/31554230533)は成功した。
- ゲームコード、ゲームルール、得点、保存形式、公開artifactは変更していない。
- 実機3端末、初見5人、Pages設定画面は未確認のため、MVP 6/7とR1/R2保留を維持する。
---

## 17. MVP後のランキング工程

ランキングはM7の完了後、利用者が開始を明示した場合だけ進める。MVPの7工程には数えない。

### R1: 公式プレイとサーバー再計算

推奨ブランチ: `agent/r1-official-run-verifier`

- Supabaseから`runId`、公式シード、`ruleVersion`、有効期限、1回限りの識別値を発行する。
- 固定更新入力を圧縮して送信する。
- サーバー側で同じゲーム判定を再生する。
- サーバー計算得点だけを保存する。
- 期限切れ、二重送信、不可能入力、版不一致、再生不一致を拒否する。
- 通常モードと公式モードを分ける。

サーバー再計算を実装しない場合、厳密な競技ランキングとは表現しない。

### R2: ランキング画面と運用

推奨ブランチ: `agent/r2-ranking-ui`

- 結果画面の下部へ上位10名を表示する。
- 同じ匿名端末は最高記録だけを上位候補にする。
- 疑わしい記録を非表示にできる運用を用意する。
- 版が違う記録を混在させない。
- 送信失敗でもゲーム結果を失わない。

---

## 18. 継続的な自動試験

### 18.1 毎Pull Request

- JavaScript構文確認
- 単体試験
- 決定性試験
- 主要なシード生成試験
- ブラウザ起動試験
- 小型横画面と縦画面案内

### 18.2 ゲーム判定を変更するPull Request

- 1,000シードの7戦略比較
- 配列順変更試験
- 保存済み再生データとの一致
- 得点配分の分布
- `simulationFault`確認

### 18.3 公開候補

- 10,000シード
- 全E2E
- GitHub Pages経路
- 10回連続プレイ
- 3端末の実機確認
- 初見外部試遊

---

## 19. Pull Request共通の完了条件

各工程は、次を満たすまで同じPull Requestで修正する。

- 目的に含めた機能が途中状態でない。
- 自動試験が成功している。
- 既存の再生データを壊した場合、理由と`ruleVersion`への影響を説明している。
- ブラウザ上の確認方法がPR本文にある。
- 実機確認が必要な項目を明記している。
- 同じ目的の未修正事項を別PRへ先送りしていない。
- 文書と実装が一致している。
- 新しい外部依存を追加した場合、必要性、版、ライセンスを記録している。

---

## 20. 変更管理

### 20.1 ルール値の変更

ゲーム結果へ影響する設定を変更した場合は、次を同じPull Requestで行う。

- 変更理由を記録する。
- 7戦略比較を再実行する。
- 保存済み再生データを更新するか、旧版として残す。
- `ruleVersion`を変更する必要があるか判断する。

### 20.2 表示だけの変更

装飾表示だけを変更した場合も、同じ入力記録の得点が変わらないことを確認する。

### 20.3 技術構成の変更

Canvas 2Dから別の描画基盤へ変更する場合は、実機計測でCanvas 2Dが完成条件を満たせないことを示してから、独立した計画変更を行う。新しい技術だからという理由だけで変更しない。

---

## 21. 現時点の未確定事項と確定時期

| 項目 | 確定工程 | 判断材料 |
|---|---|---|
| 基本爆発半径 | M4 | 連鎖率、自動戦略、試遊 |
| 選択判定半径 | M3〜M4 | 誤取得、取得失敗、全面往復 |
| 最大花火数 | M2〜M4 | 10,000シード、性能、前半放置 |
| 波の混合率 | M4 | 戦略差、60秒の体験曲線 |
| 連鎖減衰の最終値 | M4 | A/B比較、盤面全体連鎖率 |
| 得点式 | M4で候補、M7で固定 | 7戦略、試遊、得点配分 |
| 装飾粒数 | M5 | 3端末の性能 |
| MVP完成判定 | M7 | 自動、実機、初見試遊 |

未確定値を理由なく実装者の判断で固定しない。確定した値は`src/config/`と確認文書へ反映する。

---

## 22. 次に開始する作業

Pull Request [#17](https://github.com/chameleonjp-lab/hanabin/pull/17)が2026年8月12日に`main`へマージされた。マージコミットは`cd854f2c08e7f986ee97c7bd43fca0ee82b21dec`である。PR #17の変更により、GitHub Pagesの公開URL終端検査をPagesデプロイ後に実行できる状態になった。

[Deploy GitHub Pages #7](https://github.com/chameleonjp-lab/hanabin/actions/runs/31540518416)はPR #17のマージコミットを公開し、成功した。その後の[Public Release Smoke #1](https://github.com/chameleonjp-lab/hanabin/actions/runs/31540556019)も成功し、公開URLでホーム→初回練習→スキップ→カウントダウン→本編→実時間60秒→結果画面まで到達した。

```text
ブランチ: agent/m7-post-public-smoke-20260812
目的: 公開URL終端検査の証跡を同期し、残るM7確認条件を管理する
進捗: 6/7・公開確認中
```

公開URL専用検査は、テスト用の固定tick操作口を公開ページで使わず、利用者向けの操作を実時間で実行した。結果画面の二重登録がなく、JavaScriptエラー、4xx以上の応答、失敗リクエストも検出されなかった。

Pull Request [#19](https://github.com/chameleonjp-lab/hanabin/pull/19)は2026年8月11日に`main`へマージされ、マージコミットは`ce9101673126671001dd8350dc729733e4e2209c`である。M7手動受入チェックリストを追加し、実機3端末・初見5人・Pages設定画面の確認を未確認のまま記録できる状態にした。

[CI Core #45](https://github.com/chameleonjp-lab/hanabin/actions/runs/31547650607)と[CI Browser #45](https://github.com/chameleonjp-lab/hanabin/actions/runs/31547650595)は成功した。ゲームコード、ゲームルール、得点、保存形式、公開artifactは変更していない。

次は、[M7手動受入チェックリスト](./docs/M7_MANUAL_ACCEPTANCE_CHECKLIST.md)に沿って、iPhone 17 Pro、iPhone 11 Pro、iPad Pro 2018の実機確認、初見5人の試遊、Pages設定画面の公開元確認を実際の証跡で記録する。チェックリストの未確認欄が残る間はMVPを7/7へ進めない。R1公式プレイとR2ランキングは、MVP完成および利用者の明示指示まで保留する。