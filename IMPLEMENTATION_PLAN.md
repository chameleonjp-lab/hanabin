# HANABIN ブラウザゲーム実装計画書

- 作成日: 2026年8月9日
- 対象リポジトリ: `chameleonjp-lab/hanabin`
- 文書状態: 推奨方針の確定版・実装開始前
- 対象段階: 最初に遊びの成立を確認できる最小版（MVP）から公開版まで

---

## 1. 計画の目的

本作は、夜空を移動する同色の花火を照準で選び、指を離した瞬間に爆発させ、その爆発で周囲の花火を巻き込んで連鎖を伸ばす、60秒のスコアアタックゲームとする。

単に同じ色を3個そろえるゲームにはしない。どの花火を選び、どこに爆発の起点を並べ、次に出現する色へどうつなぐかを考えるゲームにする。

PlayStation 2用ソフト「FantaVision」は着想元の一つだが、名称、画面、画像、音、文章、得点式、花火模様、操作説明を流用しない。本作独自のルール、見た目、名称で制作する。

この計画では、次の順序を守る。

1. まず選択と連鎖が面白いか確認する。
2. 次に花火としての見栄えとスマートフォンでの動作を高める。
3. 得点式と出現条件を固定してから、オンラインランキングを追加する。

---

## 2. 決定事項

### 2.1 ゲームの基本条件

| 項目 | 採用内容 |
|---|---|
| 仮称 | HANABIN |
| 形式 | 1人用・60秒スコアアタック |
| 主対象 | iPhone 17 Pro |
| 確認対象 | iPhone 11 Pro、iPad Pro 2018、一般的なPCブラウザ |
| 画面方向 | 横画面を基本とする。縦画面では回転案内を表示する |
| 色数 | 4色 |
| 色の補助 | 色ごとに異なる記号を必ず付ける |
| 選択成立数 | 同色3個以上 |
| 違う色への接触 | 失敗にせず無視する |
| 爆発の巻き込み | 色に関係なく周囲の花火を巻き込む |
| 次の出現予告 | 次の2色を表示する |
| 初期公開 | GitHub Pages |
| MVPの保存 | 端末内の最高得点と設定 |
| オンラインランキング | ゲーム調整完了後にSupabaseで追加 |
| MVPの音 | 対象外。後続で任意に有効化する |

### 2.2 技術方針

- プレイ画面はCanvas 2Dで描く。
- 得点、残り時間、次の色、ボタン、説明は通常のHTMLで表示する。
- JavaScriptは機能ごとにファイルを分け、ブラウザが直接読み込める形式にする。
- MVPでは実行時の外部ライブラリを使わない。
- ゲームの判定と、見た目だけの光の粒を分ける。
- Canvas 2Dで実機性能が足りないことを計測で確認した場合だけ、描画部分をPixiJSへ交換する。

---

## 3. プレイヤーが繰り返す行動

プレイヤーは、次の流れを60秒間繰り返す。

1. 夜空を移動する花火を見る。
2. 指を置き、照準を動かす。
3. 最初に触れた花火の色を、その操作中の対象色にする。
4. 同色の花火を3個以上選ぶ。
5. 指を離して、選んだ花火を同時に爆発させる。
6. 爆発が周囲の花火へ届き、追加の爆発が起きる。
7. 次の2色の予告を見ながら、次の起点を探す。

プレイヤーには、3個ですぐ爆発させる安全な判断と、さらに多く選んで広い連鎖を狙う判断の両方を与える。長く選び続けるほどゲーム時間を使うため、選択数を増やすこと自体が危険になる。

---

## 4. ゲームルール

### 4.1 花火の状態

判定に使う花火は、次の状態を持つ。

- 出現待ち
- 飛行中
- 選択中
- 爆発待ち
- 爆発中
- 終了

一つの花火は一度だけ得点対象になる。複数の爆発へ同時に触れても、重複して得点を加えない。

### 4.2 選択

- 指を置いた位置より少し上に照準を表示し、指で対象が隠れにくいようにする。
- 最初に選んだ花火の色で、その操作中の対象色を固定する。
- 同じ色に触れると選択へ追加する。
- 違う色は無視する。選択解除や減点は行わない。
- 同じ花火へ再び触れても重複させない。
- 指を離した時点で3個以上なら爆発する。
- 2個以下なら得点なしで選択を解除する。
- 選択中も残り時間は進む。
- 選択済みの花火は、指を離すまで選択状態を保つ。

### 4.3 爆発と連鎖

選択した花火は、それぞれが爆発の起点になる。選択数が多いほど各起点の最大爆発範囲を広げる。ただし、増加量には上限を設ける。

爆発範囲へ飛行中の花火が入ると、その花火は短い遅延後に次の爆発を起こす。二次爆発以降も周囲を巻き込み、連鎖が続く。

終わらない連鎖や端末停止を防ぐため、次の安全制限を内部に持つ。

- 一つの花火は一度だけ爆発する。
- 同時に処理する爆発数に上限を設ける。
- 連鎖解決時間に上限を設ける。
- 上限へ達した場合も、すでに得点対象になった花火の得点は失わせない。

### 4.4 60秒終了時

残り時間が0になった時点で、次を行う。

- 新しい花火の出現を止める。
- 新しい指操作を受け付けない。
- 選択中で3個以上なら自動で爆発させる。
- 2個以下なら選択を解除する。
- 時間内に発生した連鎖だけを最後まで処理する。
- 連鎖解決は最大2.5秒（150更新）までとし、その後に結果を確定する。

### 4.5 得点

得点式は実装後の試遊で調整するが、初期案は次の考え方で作る。

- 花火1個ごとの基礎点
- 直接選択した個数に応じた選択ボーナス
- 連鎖の深さに応じた倍率
- 一度の操作で巻き込んだ総数に応じた追加ボーナス

初期計算案は次のとおりとする。

- 基礎点: 1個100点
- 選択ボーナス: `250 × (選択数 - 2)²`
- 連鎖倍率: `1 + 0.35 × 連鎖段階`。倍率計算上の連鎖段階は8で上限
- 得点はすべて整数に丸める

この数値は固定仕様ではない。MVPの試遊で、3個を素早く繰り返すだけの行動が最適になっていないかを確認し、必要なら変更する。

---

## 5. 出現設計と運の抑制

### 5.1 同じ条件を再現できる出現順

同じ初期値なら同じ順番を作れる乱数を使う。この方式をシード付き乱数と呼ぶ。

色、出現時刻、初期位置、移動方向、奥行きは、ゲーム開始時に一つの初期値から生成する。これにより、同じ初期値と同じ操作なら同じ結果を再現できる。

MVPではゲームごとに初期値を変える。オンラインランキングを追加するときは、同じ日や同じ大会では共通の初期値を使用できるようにする。

### 5.2 出現の制約

完全な無作為にはしない。次の制約を入れ、長時間何もできない状態を防ぐ。

- 一定時間内に、3個以上を狙える同色の組み合わせを必ず含める。
- 特定の色が長時間出ない状態を防ぐ。
- 予告した色と実際の出現色を必ず一致させる。
- 画面上の判定対象数に上限を設ける。
- 同じ場所への極端な重なりを避ける。
- 画面外へ出る直前の花火ばかりが候補にならないようにする。

### 5.3 60秒間の変化

| 時間 | 内容 |
|---|---|
| 0〜15秒 | 操作を理解できる量と速度 |
| 15〜40秒 | 通常の密度。連鎖位置を考えさせる |
| 40〜55秒 | 出現数と移動速度を少し増やす |
| 55〜60秒 | 最終局面。密度を上げるが、見分けられない量にはしない |

難しさは花火の数だけで上げない。移動方向、奥行き、色の並び、次の出現とのつながりで変化を作る。

---

## 6. 画面と操作

### 6.1 画面状態

ゲーム全体を次の6状態に分ける。

1. ホーム
2. 操作説明
3. 開始前カウント
4. プレイ中
5. 時間終了後の連鎖処理中
6. 結果

状態ごとに受け付ける操作を限定し、二重開始、結果確定後の加点、再挑戦時のデータ残りを防ぐ。

### 6.2 ホーム画面

- ゲーム名
- プレイヤー名入力と保存
- ゲーム開始
- 操作説明
- 音と演出設定の入口。MVPでは演出設定のみ
- 端末内最高得点

### 6.3 プレイ画面

- 残り時間
- 現在得点
- 現在の選択色と選択数
- 次に出る2色
- 照準
- 選択済み花火を結ぶ線または輪
- 連鎖数と最大連鎖の短い表示

得点表示は大きく動かしすぎない。花火を見る妨げにならない位置へ固定する。

### 6.4 結果画面

- 最終得点
- 最高得点更新の有無
- 最大連鎖
- 直接選択した総数
- 連鎖で巻き込んだ総数
- 再挑戦
- ホームへ戻る
- 結果共有。公開URLを文末へ含める

オンラインランキング追加後は、結果画面の下部へ上位10名を表示する。

### 6.5 スマートフォン向け対応

- 画面の横スクロールを禁止する。
- ゲーム中のピンチ拡大を無効にする。
- ゲーム中の長押しメニューを無効にする。
- 指操作はPointer Eventsで統一する。
- 操作開始後はPointer Captureを使い、指が照準から外れても操作を追跡する。
- iPhoneの画面端にある安全領域を考慮する。
- 縦画面ではゲームを開始せず、横向きへの案内を表示する。
- ブラウザが自動で横向きへ固定できない場合を前提にする。

---

## 7. 見た目の設計

### 7.1 判定用花火と演出用の粒を分ける

画面上で選択できる花火は、色、位置、速度、奥行き、状態を持つ少数のデータとして管理する。

爆発時に飛び散る光の粒は見た目だけに使う。処理が重い端末で粒を減らしても、当たり判定、連鎖、得点は変えない。

この分離により、端末性能によってゲーム結果が変わる問題を防ぐ。

### 7.2 疑似的な奥行き

本格的な3D空間は作らない。各花火に0から1の奥行き値を持たせ、次を変える。

- 表示サイズ
- 明るさ
- 軌跡の太さ
- 移動速度
- 爆発の大きさ
- 背景との重なり順

奥の花火は小さく暗く、手前の花火は大きく明るく表示する。

### 7.3 MVPの花火表現

MVPでは、次の2種類を作る。

- 放射状に広がる花火
- 円環状に広がる花火

花火の模様は見た目だけを変え、MVPでは得点性能を変えない。特殊性能は公開後の拡張候補とする。

### 7.4 光と点滅への配慮

- 色だけで花火を判別させず、記号を併記する。
- 背景と花火の明暗差を確保する。
- 画面全体を短時間に白くする演出を避ける。
- 高速で繰り返す強い点滅を使わない。
- 演出を減らす設定を用意する。

---

## 8. 技術構成

### 8.1 実行時の構成

- HTML
- CSS
- ブラウザ標準のJavaScript
- Canvas 2D
- Local Storage
- GitHub Pages

MVPの実行時依存は0件とする。外部配信サービスからJavaScriptを読み込まず、公開物はリポジトリ内で完結させる。

### 8.2 推奨ファイル構成

実装時は、次の役割で分ける。名称は実装時に多少変更してよいが、責任の分離は維持する。

```text
index.html
styles/
  base.css
  game.css
src/
  app.js
  config.js
  game/
    session.js
    loop.js
    state.js
    random.js
    spawn.js
    selection.js
    chain.js
    scoring.js
    entities.js
  render/
    canvas-renderer.js
    firework-effects.js
    particle-pool.js
  input/
    pointer-controller.js
  ui/
    screens.js
    hud.js
  storage/
    local-storage.js
tests/
  random.test.js
  chain.test.js
  scoring.test.js
  session.test.js
```

### 8.3 ゲーム更新と描画

画面表示はブラウザの画面更新に合わせる。一方、ゲームの判定は一定間隔で進める。

この一定間隔の計算を固定時間刻みと呼ぶ。内部計算は1秒を60分割した間隔を基本とし、端末が一時的に遅くなっても判定順が崩れにくい構成にする。

処理の順番は毎回固定する。

1. 入力を取り込む。
2. ゲーム時間を進める。
3. 花火を移動する。
4. 選択を更新する。
5. 爆発と巻き込みを判定する。
6. 得点を確定する。
7. 終了した対象を整理する。
8. 画面を描く。

### 8.4 主なデータ

#### GameSession

- ゲーム状態
- 開始時刻
- 残り時間
- 得点
- 現在の乱数初期値
- 最大連鎖
- 直接選択数
- 連鎖捕捉数

#### TargetFirework

- 個別番号
- 色
- 記号
- 位置
- 速度
- 奥行き
- 状態
- 選択済みか
- 得点済みか

#### SelectionSession

- 操作中か
- 対象色
- 選択した花火番号の集合
- 照準位置
- 操作開始時刻

#### ExplosionNode

- 発生元の花火番号
- 中心位置
- 現在半径
- 最大半径
- 連鎖段階
- 開始時刻
- 終了済みか

#### SpawnEvent

- 出現時刻
- 色
- 記号
- 初期位置
- 速度
- 奥行き

### 8.5 得点イベントの記録

得点は画面表示へ直接足さず、得点イベントとして一度記録してから合計する。

各イベントに、花火番号、発生理由、直接選択か連鎖か、連鎖段階、加点値を残す。これにより、二重加点の検出、結果内訳、再現試験を行える。

---

## 9. 性能設計

### 9.1 基本方針

ゲーム上の花火は減らさず、見た目の粒だけを端末性能に合わせて減らす。

### 9.2 粒の再利用

爆発ごとに新しい粒を大量作成して破棄すると、一時停止が起きやすい。使い終わった粒を再利用する仕組みを使う。この方式をオブジェクトプールと呼ぶ。

- ゲーム開始時に一定数を準備する。
- 終了した粒を未使用状態へ戻す。
- 上限を超えた場合は古い演出から消す。
- 判定用花火と爆発判定は削減しない。

### 9.3 表示品質の段階

| 段階 | 想定 | 粒の目安 | その他 |
|---|---|---:|---|
| 高 | iPhone 17 Pro相当 | 最大1,800 | 残像と発光を有効 |
| 中 | iPhone 11 Pro相当 | 最大1,000 | 発光回数を減らす |
| 低 | 処理が重い端末 | 最大600 | 残像を短くし、背景効果を減らす |

数値は初期値であり、実機計測で変更する。

### 9.4 Canvasの大きさ

端末の画面密度をそのまま使うと描画量が増えすぎるため、内部の画面密度倍率に上限を設ける。CSS上の表示サイズとCanvas内部の描画サイズを分け、ぼやけと処理負荷の両方を調整する。

### 9.5 自動品質調整

一定時間、画面更新が遅い状態が続いた場合は、次の順で演出を減らす。

1. 残像の長さ
2. 1回の爆発で出す粒数
3. 背景の光
4. 発光の重ね描き

品質を下げてもゲーム状態、出現順、爆発判定、得点は変えない。

---

## 10. 保存とランキング

### 10.1 MVPの端末内保存

次をLocal Storageへ保存する。

- プレイヤー名
- 最高得点
- 最大連鎖の最高記録
- 演出品質設定
- 操作説明を確認済みか
- 保存形式の版

保存データが壊れている場合は、安全な初期値へ戻し、ゲーム開始を妨げない。

### 10.2 オンラインランキング追加時

Supabaseへ送る候補は次のとおりとする。

- プレイヤー名
- 得点
- 最大連鎖
- 直接選択数
- 連鎖捕捉数
- ルール版
- ゲーム版
- 乱数初期値または大会識別子
- 実プレイ時間
- 記録日時

公開可能なキーだけをブラウザへ置く。記録登録はデータベース表への直接追加ではなく、Supabase側の関数を経由させる。

最低限、次をサーバー側で検査する。

- プレイ時間が規定範囲内か
- 得点が整数か
- 得点と内訳が明らかに矛盾していないか
- 同じ端末や接続元から短時間に大量送信されていないか
- 現在受け付けているゲーム版か

ブラウザゲームだけで不正を完全に防ぐことはできない。ランキングは、疑わしい記録を後から非表示にできる運用を前提とする。

---

## 11. 最初に作る最小版（MVP）

### 11.1 含める内容

- 横画面の60秒モード
- 4色と4種類の記号
- 指で照準を動かす操作
- 同色3個以上の選択
- 指を離した瞬間の爆発
- 色を問わない連鎖
- 次の2色の予告
- 得点、残り時間、最大連鎖
- ホーム、説明、カウント、ゲーム、結果
- 端末内の最高得点
- 2種類の花火表現
- 3段階の演出品質
- GitHub Pages公開

### 11.2 MVPに含めない内容

- Supabaseランキング
- 利用者アカウント
- 特殊能力を持つ花火
- 道具や強化要素
- 物語
- 複数のゲームモード
- 音と振動
- 本格的な3D描画
- 広告
- 課金

対象外の機能を先に追加せず、選択と連鎖の面白さを確認する。

---

## 12. 実装順序

作業を細かく分けすぎない。原則として、一つのPull Requestで一つの動作確認可能なまとまりを完成させる。

### 第1段階: 判定だけで遊べる試作

花火を単純な円で表示し、次を完成させる。

- ゲーム状態
- 60秒の時間管理
- 固定時間刻みの更新
- シード付き出現順
- 花火の移動
- 同色選択
- 指を離した爆発
- 連鎖判定
- 仮得点

**完了条件:** 見た目が仮でも、60秒遊んで結果まで進める。

### 第2段階: スマートフォン操作と戦略性

- 指より上に照準を表示
- 選択色と選択数を明示
- 違う色を無視
- 次の2色を予告
- 選択数による爆発範囲の変化
- 時間終了時の処理
- 横画面案内

**完了条件:** 指で隠れて選べない、誤接触だけで失敗する、何を選んだか分からない問題がない。

### 第3段階: 花火表現と性能対策

- 軌跡
- 放射状と円環状の爆発
- 疑似的な奥行き
- 粒の再利用
- 表示品質の自動調整
- 画面密度倍率の制限

**完了条件:** iPhone 17 Proで通常時は滑らかに動き、大連鎖時にも長い停止が起きない。iPhone 11 Proでは演出を減らして遊べる。

### 第4段階: 画面、保存、結果

- ホーム
- 操作説明
- プレイヤー名保存
- 結果内訳
- 最高得点保存
- 再挑戦
- 共有文

**完了条件:** 初めて開いた人が説明を読んで開始し、結果確認と再挑戦まで迷わず進める。

### 第5段階: 品質確認とMVP公開

- 自動試験
- 実機確認
- 10回連続プレイ
- 画面回転と中断復帰
- 得点式と出現量の調整
- GitHub Pages公開確認

**完了条件:** 後述のMVP完成条件をすべて満たす。

### 第6段階: ランキング拡張

MVPの得点式と出現条件を固定した後に実施する。

- Supabase表と登録関数
- ルール版とゲーム版の分離
- 上位10名表示
- 不自然な記録の拒否
- 記録の非表示運用
- 公式シード

**完了条件:** 異なるルール版の記録が混ざらず、結果画面から登録と閲覧ができる。

---

## 13. 試験計画

### 13.1 自動試験

純粋なゲーム判定はCanvasや画面部品から分離し、次を自動試験する。

- 同じ初期値で同じ出現順になる。
- 予告色と出現色が一致する。
- 2個以下の選択で得点しない。
- 3個以上の同色選択で爆発する。
- 違う色を選択へ追加しない。
- 同じ花火を二重選択しない。
- 同じ花火を二重加点しない。
- 爆発範囲の境界で判定が安定する。
- 連鎖段階と得点が一致する。
- 残り時間0以降に新規操作を受け付けない。
- 再挑戦時に前回の花火や得点が残らない。
- 保存データ破損時に安全に初期化する。

同じ操作記録を再生し、同じ最終得点になる試験も用意する。

### 13.2 実機試験

#### iPhone 17 Pro

- Safari
- 横画面
- 通常品質
- 10回連続プレイ
- 大連鎖
- ホーム画面へ移動後の復帰
- 通知表示後の復帰

#### iPhone 11 Pro

- Safari
- 中または低品質への自動変更
- 大連鎖時の停止
- 入力遅延

#### iPad Pro 2018

- 画面中央配置
- 余白と安全領域
- 花火密度が広い画面で薄く見えないか
- 指移動距離が長すぎないか

#### PCブラウザ

- マウス操作
- ウィンドウ大きさ変更
- Chrome、Safari相当、Firefox相当の基本確認

### 13.3 性能の合格基準

- iPhone 17 Proでは通常時に60fps付近を目標とする。
- 大連鎖時も250ミリ秒を超える停止を繰り返さない。
- iPhone 11 Proでは30fps未満の状態が長時間続かない。
- 10回連続で遊んでも、ゲーム開始時の花火数や未使用粒数へ戻る。
- 連続プレイによって明らかに入力が遅くならない。
- 品質変更で得点、出現順、連鎖結果が変わらない。

---

## 14. MVP完成条件

次をすべて満たした時点で、MVP完成とする。

### 14.1 遊び

- 60秒間、選択と連鎖を繰り返せる。
- 3個で早く爆発させる判断と、多く選ぶ判断の両方に意味がある。
- 次の2色の予告を使った先読みが成立する。
- 運だけで大きく得点が決まらない。
- 連鎖が偶然起きるだけでなく、起点位置を考える余地がある。

### 14.2 正しさ

- 同じ花火を二重に得点化しない。
- 予告と実際の色が一致する。
- 残り時間0以降の新規操作を拒否する。
- 時間内に始まった連鎖を規定どおり解決する。
- 同じ初期値と同じ操作で、出現順、連鎖数、得点が一致する。

### 14.3 操作

- 指の下に照準や重要情報が隠れない。
- 選択色、選択数、選択済み花火が分かる。
- 違う色への誤接触だけで操作全体が失敗しない。
- 画面端でも照準を操作できる。
- 画面回転や中断復帰で操作が固定されたままにならない。

### 14.4 性能

- 対象3端末で最後までプレイできる。
- 大連鎖でブラウザが停止または再読み込みしない。
- 演出品質を下げてもゲーム結果が変わらない。
- 10回連続プレイで目立った悪化がない。

### 14.5 画面と説明

- 初回利用者が、同色を3個以上選び指を離す操作を理解できる。
- ホーム、ゲーム、結果の内容が画面からはみ出さない。
- 色と記号の両方で花火を識別できる。
- 結果から再挑戦できる。
- 共有文の末尾に公開URLが入る。

### 14.6 権利と依存関係

- FantaVisionの画像、音、文章、名称、画面を流用しない。
- ライセンス不明のリポジトリからコードや素材をコピーしない。
- 外部コードを導入した場合は、対象版とライセンスを記録する。
- MVPの実行時依存は0件である。

---

## 15. 主な危険と対策

| 危険 | 影響 | 対策 |
|---|---|---|
| 粒が多すぎて重くなる | 入力遅延、停止 | 判定と演出を分離し、粒だけを減らす |
| 指で花火が隠れる | 誤操作 | 照準を指より上へ表示する |
| 3個を連打するだけになる | 戦略性不足 | 選択数で爆発範囲と選択ボーナスを増やす |
| 出現運で得点差が決まる | 競技性低下 | シード付き出現順と出現制約を使う |
| 連鎖が終わらない | 結果確定不能 | 一度だけの爆発、処理数と時間の上限 |
| 色を見分けにくい | 遊べない利用者が出る | 色ごとに記号を付ける |
| ライブラリがゲーム判定を隠す | 修正困難 | MVPは外部実行時依存なし |
| 古いOSSをそのまま使う | 保守、互換、権利問題 | 設計だけ参考にし、現在の構成で作り直す |
| 先にランキングを入れる | 調整前の記録が残る | 得点式を固定してから追加する |
| 画面外への誤操作 | 選択が解除されない | Pointer Captureと中断時の強制解除 |

---

## 16. OSS調査の反映

調査した主な公開リポジトリと、本計画での扱いは次のとおり。

| リポジトリ | 参考にする内容 | 採用判断 |
|---|---|---|
| https://github.com/crashmax-dev/fireworks-js | 軌跡、重力、明るさ、軽量な花火設定 | 設計参考のみ |
| https://github.com/tsparticles/tsparticles | 機能を分割して必要分だけ読み込む考え方 | MVPには大きすぎるため不採用 |
| https://github.com/catdad/canvas-confetti | Canvas再利用、粒数、動きを減らす設定 | 結果演出の将来候補 |
| https://github.com/pixijs-userland/particle-emitter | 粒の色、速度、寿命、発生形状の分離 | PixiJS版の違いがあるためMVP不採用 |
| https://github.com/pixijs/pixijs | 大量の2D描画 | Canvas 2D不足時の交換候補 |
| https://github.com/phaserjs/phaser | 状態、入力、時間管理の整理 | 今回には機能が多いため不採用 |
| https://github.com/straker/kontra | 小規模ゲームの機能分割 | 設計参考のみ |
| https://github.com/deciduously/dots | 判定と描画の分離、連鎖状態 | 設計参考のみ |
| https://github.com/jeannforbes/Boomshine | 経過時間での移動、爆発状態、端末内最高得点 | 古いためコード土台にはしない |
| https://github.com/aolde/chain-reaction | 円、進行、画面、得点の分離 | 古い依存関係を含むため設計参考のみ |

MITやISCなど再利用可能なライセンスであっても、MVPでは原則としてコードを直接取り込まない。取り込む場合は、必要性、対象版、改変範囲、ライセンス表示をPull Requestへ記録する。

ライセンス表記が確認できない類似リポジトリは、コードと素材を再利用しない。

---

## 17. 開発とPull Requestの運用

- `main`への直接実装は行わない。
- 実装は作業用ブランチで行う。
- 原則としてDraft Pull Requestを作成する。
- 一つのPull Requestは、実機またはブラウザで一まとまりとして確認できる範囲にする。
- 小さな内部変更だけでPull Requestを増やしすぎない。
- 自動試験が失敗している状態で、別の修正Pull Requestを重ねない。
- 実装内容、確認方法、残る危険をPull Request本文に記載する。
- Ready化とマージは、利用者の確認後に行う。

推奨する大きなPull Request単位は、前述した第1段階から第6段階を基本とする。ただし一つの差分が大きすぎる場合は、動作確認可能な境界でのみ分割する。

---

## 18. MVP後の拡張候補

MVP完成後は、遊びの内容と性能計測を見て次を検討する。

- 日替わり共通シードの公式モード
- Supabaseランキング
- 練習モード
- 花火ごとの特殊な爆発形状
- 連鎖方向を変えるフィールド効果
- 音と振動。初期設定は無効、利用者が任意で有効化
- 連鎖記録の短い再生
- 大会用の固定出現順
- 低い動き設定
- Canvas 2DからPixiJSへの描画交換

拡張では、特殊花火を増やすだけでなく、「どの起点を今使い、どの花火を次へ残すか」という判断を増やすことを優先する。

---

## 19. 実装開始条件

実装開始時は、次を前提とする。

- 本計画書を基準仕様とする。
- 第1段階から開始する。
- 初回実装では花火を単純な円で表示し、見た目を先に作らない。
- 得点式の数値は仮値として扱い、試遊前に固定しない。
- オンラインランキングはMVP完成後まで実装しない。
- 実装中に計画と異なる判断が必要になった場合は、理由と影響をPull Requestへ記録する。

以上。
