{
  "name": "hanabin",
  "version": "0.1.0",
  "private": true,
  "description": "HANABIN browser game foundation",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "test:syntax": "node scripts/check-syntax.mjs",
    "test:browser": "playwright test",
    "test:all": "npm run test:syntax && npm test && npm run test:browser",
    "serve": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
